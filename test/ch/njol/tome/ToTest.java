package ch.njol.tome;

public class ToTest {
	
	// parse every kind of AST elements from different strings, and test:
	// - parses with/without errors- as expected
	// - parsed data is correct
	// - parsed AST is as expected (including ordering)
	// - writing the AST back to a String results in an exact match
	
}
